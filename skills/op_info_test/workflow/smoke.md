<a id="op-info-smoke-steps"></a>
1. check the agent permission. Ask for full permission if it's not given.
2. checkout commit c3099e48e
3. execute the following command: 
> add test for mint.eye. The test code is already added in the HEAD commit, so skip the first two steps and starting from patching out old tests. 
> Don't ask until finishing the whole process(to the last step)
